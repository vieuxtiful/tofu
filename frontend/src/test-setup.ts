// vitest global setup — happy-dom provides document/window/requestAnimationFrame.
// @testing-library/jest-dom matchers are not installed (we assert on DOM
// properties directly), so this file is intentionally minimal.
export {};
